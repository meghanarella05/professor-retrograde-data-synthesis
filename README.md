# professor-retrograde-data-synthesis

Vedic astrology chart engine scaffold with a FastAPI backend and starter frontend utilities.

## Backend setup

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Run backend tests

```bash
cd backend
pytest -q
```
